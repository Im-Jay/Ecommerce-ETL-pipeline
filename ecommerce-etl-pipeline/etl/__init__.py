"""
ETL Pipeline package for E-Commerce Data Analytics.

Modules:
    extract  — Read and validate raw CSV files
    transform — Clean, validate, and enrich data
    load     — Insert cleaned data into PostgreSQL
"""
